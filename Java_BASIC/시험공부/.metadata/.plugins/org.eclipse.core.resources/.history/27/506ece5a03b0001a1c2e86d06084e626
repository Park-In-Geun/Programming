package Class06;

public class CellPhone {
	static int count; // 핸드폰 수량
	protected String telNum; // 폰번호
	boolean power; // on/off
	
	CellPhone(String n){
		this.telNum = n;
		this.power = true;
		count++;
	}
	
	int getCount(){
		return count;
	}
	
	boolean powerToggle(){
		if(this.power)
			return !this.power;
		return this.power;
	}
	
	public String toString(){
		return "전화번호 : " + this.telNum + "\n전원 상태 : " + this.power + "\n핸드폰 전체 수량 :" + getCount();
	}
	
}
